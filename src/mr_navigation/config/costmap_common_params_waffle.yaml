# Waffle 车体近似为 0.266m x 0.266m 的方形底盘，footprint 略放大以覆盖轮距外沿。
obstacle_range: 3.0
raytrace_range: 3.5

footprint: [[-0.140, -0.153], [-0.140, 0.153], [0.140, 0.153], [0.140, -0.153]]

inflation_radius: 0.45
cost_scaling_factor: 3.0

observation_sources: scan
scan:
  sensor_frame: base_scan
  data_type: LaserScan
  topic: /scan
  marking: true
  clearing: true

obstacle_layer:
  obstacle_range: 3.0
  raytrace_range: 3.5
  observation_sources: scan
  scan:
    sensor_frame: base_scan
    data_type: LaserScan
    topic: /scan
    marking: true
    clearing: true

inflation_layer:
  inflation_radius: 0.45
  cost_scaling_factor: 3.0
