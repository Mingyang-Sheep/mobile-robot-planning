<launch>
  <!-- model 是多底盘入口，负责把导航层绑定到对应的底盘专有参数文件。 -->
  <arg name="model" default="burger"/>
  <!-- map_name 是导航地图入口，可选择 turtlebot3_world、maze_2 或 maze_2_hector。 -->
  <arg name="map_name" default="turtlebot3_world"/>
  <arg name="scan_topic" default="/scan"/>
  <arg name="odom_topic" default="/odom"/>
  <arg name="cmd_vel_topic" default="/cmd_vel"/>
  <arg name="move_forward_only" default="false"/>
  <arg name="odom_model_type" default="diff"/>
  <arg name="controller_frequency" default="10.0"/>
  <arg name="base_global_planner" default="navfn/NavfnROS"/>
  <arg name="traditional_goal_topic"
       default="/mr_traditional_planner/planner_goal"/>
  <arg name="traditional_path_topic"
       default="/mr_traditional_planner/optimal_path"/>
  <arg name="traditional_plan_timeout" default="5.0"/>
  <!-- 默认起始点服务于 TurtleBot3 示例；Go2W 入口会覆盖为 maze_2_hector 对应初始位姿。 -->
  <arg name="initial_pose_x" default="-2.0"/>
  <arg name="initial_pose_y" default="-0.5"/>
  <arg name="initial_pose_a" default="0.0"/>

  <include file="$(find mr_maps)/launch/map_loader.launch">
    <arg name="map_name" value="$(arg map_name)"/>
  </include>

  <include file="$(find mr_navigation)/launch/amcl.launch">
    <arg name="scan_topic" value="$(arg scan_topic)"/>
    <arg name="odom_model_type" value="$(arg odom_model_type)"/>
    <arg name="initial_pose_x" value="$(arg initial_pose_x)"/>
    <arg name="initial_pose_y" value="$(arg initial_pose_y)"/>
    <arg name="initial_pose_a" value="$(arg initial_pose_a)"/>
  </include>

  <include file="$(find mr_navigation)/launch/move_base.launch">
    <arg name="model" value="$(arg model)"/>
    <arg name="cmd_vel_topic" value="$(arg cmd_vel_topic)"/>
    <arg name="odom_topic" value="$(arg odom_topic)"/>
    <arg name="move_forward_only" value="$(arg move_forward_only)"/>
    <arg name="controller_frequency" value="$(arg controller_frequency)"/>
    <arg name="base_global_planner" value="$(arg base_global_planner)"/>
    <arg name="traditional_goal_topic"
         value="$(arg traditional_goal_topic)"/>
    <arg name="traditional_path_topic"
         value="$(arg traditional_path_topic)"/>
    <arg name="traditional_plan_timeout"
         value="$(arg traditional_plan_timeout)"/>
  </include>
</launch>
